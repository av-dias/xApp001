import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tagGroupContainerStyle: {
    flexDirection: "row",
    alignItems: "center",
    display: "flex",
    flexWrap: "wrap",
    gap: 5,
  },
});
